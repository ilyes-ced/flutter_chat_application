class User {
  final int id;
  final String username, email, profileImage, token;

  const User({
    required this.id,
    required this.username,
    required this.email,
    required this.profileImage,
    required this.token,
  });
}
